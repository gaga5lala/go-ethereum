language: go
go_import_path: github.com/ethereum/go-ethereum
sudo: false
jobs:
  include:
    # These are the latest Go versions.
    - stage: build
      os: linux
      dist: xenial
      go: 1.12.7
      script:
        - go run build/ci.go install
        - go run build/ci.go test -coverage $TEST_PACKAGES
    - stage: Develop Release
      script:
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin quay.io/amis
      - make docker
      - make docker.push
      if: branch = feature/indexer_diff_1.9.2 AND (NOT type IN (pull_request))
    - stage: Staging Release
      script:
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin quay.io/amis
      - make docker
      - make docker.push
      if: tag =~ ^indexer_diff_1.9.2/v
      env: DOCKER_IMAGE_TAG="$TRAVIS_TAG"
